# AI Configuration
# Choose your AI provider: 'openai', 'lmstudio', or 'openai-compatible'
AI_PROVIDER=lmstudio

# OpenAI Configuration (only needed if AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# LM Studio Configuration (only needed if AI_PROVIDER=lmstudio)
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=local-model

# OpenAI-Compatible API Configuration (only needed if AI_PROVIDER=openai-compatible)
OPENAI_BASE_URL=http://localhost:8000/v1
# OPENAI_API_KEY=your_api_key_if_required

# AI Model Parameters
AI_TEMPERATURE=0.1
AI_MAX_TOKENS=1000

# Application Configuration
SECRET_KEY=your-secret-key-change-this-in-production
DATABASE_URL=sqlite:///./test.db